# LinkedIn post — short hook + link

Tone: professional, ~4 sentences, leads with a concrete pain point that resonates with anyone who's integrated with ST. The OG card preview from GitHub renders the repo banner automatically when you paste the URL.

---

Spent six months writing against the ServiceTitan Pricebook API — and learned the hard way that POST returns 200 OK while silently dropping half your fields.

Just open-sourced everything I learned about that catalog of pain (and the patterns that catch it before it hits production) as a Claude Code skill.

If you integrate with ServiceTitan, this might save you a few quarters of debugging.

MIT-licensed, contributions welcome: <repo URL>

#ServiceTitan #HomeServices #ClaudeCode #API #FieldServiceManagement

---

## Notes for posting

- Replace `<repo URL>` with the actual GitHub link
- LinkedIn auto-renders GitHub's OG card preview when you paste the URL — the banner image will show up below the post
- Post during weekday business hours (Tue–Thu, 8–11am or 1–3pm ET) for best engagement
- Consider tagging anyone you've worked with in the ST integrator space — but only if relevant; tagging for reach alone usually backfires
- Optional: cross-post a slightly different version to X/Twitter with the same hashtags
